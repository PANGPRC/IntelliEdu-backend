apiVersion: v1
kind: Service
metadata:
  name: intelliedu-application-service
  namespace: intelliedu
spec:
  selector:
    app: intelliedu-application-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
