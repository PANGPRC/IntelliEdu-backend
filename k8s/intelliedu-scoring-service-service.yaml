apiVersion: v1
kind: Service
metadata:
  name: intelliedu-scoring-service
  namespace: intelliedu
spec:
  selector:
    app: intelliedu-scoring-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
